export type ProfileType = {}

const profileInitialState: ProfileType = {}

export const profileReducer = (state: ProfileType = profileInitialState, action: any): ProfileType => {
    switch (action.type) {
        default:
            return state
    }
}